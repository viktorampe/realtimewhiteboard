import { SearchPortalDirective } from './search-portal.directive';

describe('SearchPortalDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchPortalDirective();
    expect(directive).toBeTruthy();
  });
});
