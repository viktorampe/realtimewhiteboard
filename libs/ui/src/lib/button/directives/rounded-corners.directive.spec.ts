import { RoundedCornersDirective } from './rounded-corners.directive';

describe('RoundedCornersDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundedCornersDirective();
    expect(directive).toBeTruthy();
  });
});
