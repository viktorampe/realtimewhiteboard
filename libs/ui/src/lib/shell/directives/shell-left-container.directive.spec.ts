import { ShellLeftContainerDirective } from './shell-left-container.directive';

describe('ShellLeftContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ShellLeftContainerDirective();
    expect(directive).toBeTruthy();
  });
});
