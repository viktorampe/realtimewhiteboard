import { ShellBottomDirective } from './shell-bottom.directive';

describe('ShellBottomDirective', () => {
  it('should create an instance', () => {
    const directive = new ShellBottomDirective();
    expect(directive).toBeTruthy();
  });
});
