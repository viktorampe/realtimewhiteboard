import { ShellTopContainerDirective } from './shell-top-container.directive';

describe('ShellTopContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ShellTopContainerDirective();
    expect(directive).toBeTruthy();
  });
});
