import { SideSheetActionsDirective } from './side-sheet-actions.directive';

describe('SideSheetActionsDirective', () => {
  beforeEach(() => {});
  it('should create an instance', () => {
    const directive = new SideSheetActionsDirective();
    expect(directive).toBeTruthy();
  });
});
