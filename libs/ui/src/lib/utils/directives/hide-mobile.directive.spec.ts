import { HideMobileDirective } from './hide-mobile.directive';

describe('HideMobileDirective', () => {
  it('should create an instance', () => {
    const directive = new HideMobileDirective();
    expect(directive).toBeTruthy();
  });
});
