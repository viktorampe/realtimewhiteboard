import { HideDesktopDirective } from './hide-desktop.directive';

describe('HideDesktopDirective', () => {
  it('should create an instance', () => {
    const directive = new HideDesktopDirective();
    expect(directive).toBeTruthy();
  });
});
